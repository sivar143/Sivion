import {Component,OnInit,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {SalesOrderApi,SalesOrder,SalesOrderItem} from './sales-order-api.service'; import {CrmApi,Customer,Opportunity} from './crm-api.service'; import {InventoryApi,Material} from './inventory-api.service';
@Component({selector:'app-sales-orders',standalone:true,imports:[CommonModule,FormsModule],template:`<section class="panel"><div class="toolbar"><div><h2>Sales Orders</h2><p>Convert qualified opportunities into executable customer orders.</p></div><button (click)="newOrder=true">+ New sales order</button></div><div class="cards"><article><span>Total orders</span><strong>{{orders.length}}</strong></article><article><span>Draft</span><strong>{{count('DRAFT')}}</strong></article><article><span>Confirmed</span><strong>{{count('CONFIRMED')}}</strong></article><article><span>Order value</span><strong>₹{{total|number:'1.0-0'}}</strong></article></div><table><thead><tr><th>Order</th><th>Customer</th><th>Opportunity</th><th>Items</th><th>Value</th><th>Status</th><th>Action</th></tr></thead><tbody><tr *ngFor="let o of orders"><td><b>{{o.orderNumber}}</b><br><small>{{o.orderDate|date:'mediumDate'}}</small></td><td>{{customerName(o.customerId)}}</td><td>{{opportunityName(o.opportunityId)}}</td><td>{{o.items.length}}</td><td>₹{{o.totalAmount|number:'1.0-2'}}</td><td><span class="pill">{{o.status}}</span></td><td><select *ngIf="o.status!=='CANCELLED'&&o.status!=='DISPATCHED'" [ngModel]="o.status" (ngModelChange)="changeStatus(o,$event)"><option *ngFor="let s of statuses">{{s}}</option></select></td></tr></tbody></table><div class="empty" *ngIf="!orders.length">No sales orders yet.</div><div *ngIf="newOrder" class="modal"><form (ngSubmit)="save()"><h2>New sales order</h2><select required [(ngModel)]="draft.customerId" name="customer"><option [ngValue]="0">Select customer</option><option *ngFor="let c of customers" [ngValue]="c.id">{{c.name}}</option></select><select [(ngModel)]="draft.opportunityId" name="opportunity"><option [ngValue]="undefined">No opportunity</option><option *ngFor="let o of opportunities" [ngValue]="o.id">{{o.name}}</option></select><input placeholder="Delivery address" [(ngModel)]="draft.deliveryAddress" name="address"><h3>Order lines</h3><div *ngFor="let item of draft.items;let i=index" class="line"><select required [(ngModel)]="item.materialId" [name]="'material'+i"><option [ngValue]="0">Select material</option><option *ngFor="let m of materials" [ngValue]="m.id">{{m.sku}} — {{m.name}}</option></select><input required type="number" min="0.0001" [(ngModel)]="item.quantity" [name]="'qty'+i" placeholder="Qty"><input required type="number" min="0" [(ngModel)]="item.unitPrice" [name]="'price'+i" placeholder="Unit price"><button type="button" (click)="removeLine(i)" *ngIf="draft.items.length>1">Remove</button></div><button type="button" (click)="addLine()">+ Add line</button><textarea placeholder="Notes" [(ngModel)]="draft.notes" name="notes"></textarea><div class="actions"><button type="button" (click)="newOrder=false">Cancel</button><button type="submit">Create order</button></div></form></div></section>`})
export class SalesOrderComponent implements OnInit{api=inject(SalesOrderApi);crm=inject(CrmApi);inventory=inject(InventoryApi);orders:SalesOrder[]=[];customers:Customer[]=[];opportunities:Opportunity[]=[];materials:Material[]=[];newOrder=false;statuses=['DRAFT','CONFIRMED','RESERVED','PARTIALLY_DISPATCHED','DISPATCHED','CANCELLED'];draft:{customerId:number;opportunityId?:number;deliveryAddress:string;notes:string;items:SalesOrderItem[]}={customerId:0,deliveryAddress:'',notes:'',items:[{materialId:0,quantity:1,unitPrice:0}]};async ngOnInit(){await this.reload();}async reload(){[this.orders,this.customers,this.opportunities,this.materials]=await Promise.all([this.api.list(),this.crm.customers(''),this.crm.opportunities(),this.inventory.materials()]);}get total(){return this.orders.reduce((a,o)=>a+Number(o.totalAmount||0),0);}count(s:string){return this.orders.filter(o=>o.status===s).length;}customerName(id:number){return this.customers.find(c=>c.id===id)?.name||`Customer #${id}`;}opportunityName(id?:number){return id?this.opportunities.find(o=>o.id===id)?.name||`Opportunity #${id}`:'—';}addLine(){this.draft.items.push({materialId:0,quantity:1,unitPrice:0});}removeLine(i:number){this.draft.items.splice(i,1);}async save(){await this.api.create(this.draft);this.newOrder=false;this.draft={customerId:0,deliveryAddress:'',notes:'',items:[{materialId:0,quantity:1,unitPrice:0}]};await this.reload();}async changeStatus(o:SalesOrder,status:string){try{await this.api.updateStatus(o.id!,status);await this.reload();}catch(e){alert(e instanceof Error?e.message:'Unable to update order');await this.reload();}}}
