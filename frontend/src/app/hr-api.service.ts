import {Injectable,inject} from '@angular/core';
import {HttpClient,HttpHeaders} from '@angular/common/http'; import {firstValueFrom} from 'rxjs'; import {AuthService} from './auth.service';
export interface Employee {id?:number; employeeNumber:string; firstName:string; lastName:string; email:string; phone?:string; departmentId?:number; designation?:string; managerId?:number; joiningDate?:string; status?:string;}
export interface Department {id?:number; code:string; name:string; parentId?:number; status?:string;}
export interface Attendance {id?:number; employeeId:number; attendanceDate:string; status:string; checkIn?:string; checkOut?:string;}
export interface LeaveRequest {id?:number; employeeId:number; leaveType:string; startDate:string; endDate:string; status?:string; reason?:string;}
export interface Goal {id?:number; employeeId:number; title:string; description?:string; targetValue?:number; currentValue?:number; dueDate?:string; status?:string;}
@Injectable({providedIn:'root'}) export class HrApi {private http=inject(HttpClient);private auth=inject(AuthService);private async options(){return{headers:new HttpHeaders({'Authorization':`Bearer ${await this.auth.token()}`,'X-Tenant-ID':'1'})};}async employees(){return firstValueFrom(this.http.get<Employee[]>('/api/v1/hr/employees',await this.options()));}async departments(){return firstValueFrom(this.http.get<Department[]>('/api/v1/hr/departments',await this.options()));}async createEmployee(e:Employee){return firstValueFrom(this.http.post<Employee>('/api/v1/hr/employees',e,await this.options()));}async createDepartment(d:Department){return firstValueFrom(this.http.post<Department>('/api/v1/hr/departments',d,await this.options()));}async attendance(){return firstValueFrom(this.http.get<Attendance[]>('/api/v1/hr/attendance',await this.options()));}async markAttendance(a:Attendance){return firstValueFrom(this.http.post<Attendance>('/api/v1/hr/attendance',a,await this.options()));}async leaves(){return firstValueFrom(this.http.get<LeaveRequest[]>('/api/v1/hr/leaves',await this.options()));}async requestLeave(l:LeaveRequest){return firstValueFrom(this.http.post<LeaveRequest>('/api/v1/hr/leaves',l,await this.options()));}async updateLeave(id:number,status:string){return firstValueFrom(this.http.patch<LeaveRequest>(`/api/v1/hr/leaves/${id}?status=${status}`,{},await this.options()));}async goals(){return firstValueFrom(this.http.get<Goal[]>('/api/v1/hr/goals',await this.options()));}async createGoal(g:Goal){return firstValueFrom(this.http.post<Goal>('/api/v1/hr/goals',g,await this.options()));}}
