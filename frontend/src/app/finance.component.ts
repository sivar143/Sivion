import {Component,OnInit,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {HttpClient,HttpHeaders} from '@angular/common/http'; import {firstValueFrom} from 'rxjs'; import {AuthService} from './auth.service'; import {CrmApi,Customer} from './crm-api.service';
@Component({selector:'app-finance',standalone:true,imports:[CommonModule,FormsModule],template:`<div class="panel"><div class="toolbar"><div><h2>Finance</h2><p>Invoices, payments and expenses with customer-linked billing.</p></div><button (click)="mode='invoice'">+ Invoice</button></div><div class="cards"><article><span>Invoices</span><strong>{{invoices.length}}</strong><small>Issued invoices</small></article><article><span>Invoiced</span><strong>₹{{invoiceTotal|number:'1.0-2'}}</strong><small>Total invoice value</small></article><article><span>Collected</span><strong>₹{{paymentTotal|number:'1.0-2'}}</strong><small>Recorded payments</small></article><article><span>Outstanding</span><strong>₹{{outstanding|number:'1.0-2'}}</strong><small>Invoice balance</small></article></div><div class="toolbar sub"><h3>Invoices</h3><button (click)="mode='invoice'">+ Invoice</button></div><table><thead><tr><th>Invoice</th><th>Customer</th><th>Amount</th><th>Status</th><th>Date</th></tr></thead><tbody><tr *ngFor="let i of invoices"><td><b>{{i.invoiceNumber}}</b></td><td>{{customerName(i.customerId)}}</td><td>₹{{i.amount|number:'1.0-2'}}</td><td><span class="pill">{{i.status}}</span></td><td>{{i.invoiceDate|date:'mediumDate'}}</td></tr></tbody></table><div class="toolbar sub"><h3>Payments</h3><button (click)="mode='payment'">+ Payment</button></div><table><thead><tr><th>Payment</th><th>Invoice</th><th>Amount</th><th>Method</th><th>Date</th></tr></thead><tbody><tr *ngFor="let p of payments"><td><b>{{p.paymentNumber}}</b></td><td>{{invoiceName(p.invoiceId)}}</td><td>₹{{p.amount|number:'1.0-2'}}</td><td>{{p.method}}</td><td>{{p.paidAt|date:'medium'}}</td></tr></tbody></table><div class="toolbar sub"><h3>Expenses</h3><button (click)="mode='expense'">+ Expense</button></div><table><thead><tr><th>Expense</th><th>Category</th><th>Description</th><th>Amount</th><th>Status</th></tr></thead><tbody><tr *ngFor="let e of expenses"><td><b>{{e.expenseNumber}}</b></td><td>{{e.category}}</td><td>{{e.description}}</td><td>₹{{e.amount|number:'1.0-2'}}</td><td><span class="pill">{{e.status}}</span></td></tr></tbody></table></div><section *ngIf="mode" class="modal"><form (ngSubmit)="submit()"><h2>{{mode==='invoice'?'New invoice':mode==='payment'?'Record payment':'New expense'}}</h2><ng-container *ngIf="mode==='invoice'"><select required [(ngModel)]="draft.customerId" name="customerId"><option [ngValue]="0">Select customer</option><option *ngFor="let c of customers" [ngValue]="c.id">{{c.name}} ({{c.code}})</option></select><input required type="number" min="0.01" placeholder="Amount" [(ngModel)]="draft.amount" name="amount"></ng-container><ng-container *ngIf="mode==='payment'"><select required [(ngModel)]="draft.invoiceId" name="invoiceId"><option [ngValue]="0">Select invoice</option><option *ngFor="let i of unpaidInvoices" [ngValue]="i.id">{{i.invoiceNumber}} — ₹{{i.amount}}</option></select><input required type="number" min="0.01" placeholder="Amount" [(ngModel)]="draft.amount" name="amount"><select [(ngModel)]="draft.method" name="method"><option>BANK_TRANSFER</option><option>CARD</option><option>CASH</option><option>OTHER</option></select></ng-container><ng-container *ngIf="mode==='expense'"><input required placeholder="Category" [(ngModel)]="draft.category" name="category"><input required placeholder="Description" [(ngModel)]="draft.description" name="description"><input required type="number" min="0.01" placeholder="Amount" [(ngModel)]="draft.amount" name="amount"></ng-container><div class="actions"><button type="button" (click)="mode=''">Cancel</button><button type="submit">Save</button></div></form></section>`,styles:[`.sub{margin-top:28px}`]})
export class FinanceComponent implements OnInit {http=inject(HttpClient);auth=inject(AuthService);crm=inject(CrmApi);base='/api/v1/finance';invoices:any[]=[];payments:any[]=[];expenses:any[]=[];customers:Customer[]=[];mode='';draft:any={};async ngOnInit(){await this.reload();this.customers=await this.crm.customers('');}async headers(){return{headers:new HttpHeaders({'Authorization':`Bearer ${await this.auth.token()}`,'X-Tenant-ID':'1'})}}async reload(){const h=await this.headers();[this.invoices,this.payments,this.expenses]=await Promise.all([firstValueFrom(this.http.get<any[]>(this.base+'/invoices',h)),firstValueFrom(this.http.get<any[]>(this.base+'/payments',h)),firstValueFrom(this.http.get<any[]>(this.base+'/expenses',h))]);}get invoiceTotal(){return this.invoices.reduce((a,x)=>a+(x.amount||0),0)}get paymentTotal(){return this.payments.reduce((a,x)=>a+(x.amount||0),0)}get expenseTotal(){return this.expenses.reduce((a,x)=>a+(x.amount||0),0)}get outstanding(){return Math.max(0,this.invoiceTotal-this.paymentTotal)}get unpaidInvoices(){return this.invoices.filter(x=>x.status!=='PAID')}customerName(id:number){return this.customers.find(c=>c.id===id)?.name||`Customer #${id}`;}invoiceName(id:number){return this.invoices.find(i=>i.id===id)?.invoiceNumber||`Invoice #${id}`;}async submit(){try{const h=await this.headers();const path=this.mode==='invoice'?'invoices':this.mode==='payment'?'payments':'expenses';await firstValueFrom(this.http.post(this.base+'/'+path,this.draft,h));this.mode='';this.draft={};await this.reload();}catch(e:any){alert(e?.error?.message||'Request failed')}}}
