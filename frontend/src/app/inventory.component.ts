import {Component,inject,OnInit} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {InventoryApi,Material,Warehouse,InventoryTransaction} from './inventory-api.service'; import {CrmApi,Customer} from './crm-api.service';
interface DispatchLine {productId:number;warehouseId:number;quantity:number}
@Component({selector:'app-inventory',standalone:true,imports:[CommonModule,FormsModule],template:`<div class="panel"><div class="toolbar"><div><h2>Inventory</h2><p>Materials, warehouse stock, ledger transactions and customer dispatches.</p></div><div><button *ngIf="canManage" (click)="mode='warehouse'">+ Warehouse</button><button *ngIf="canManage" (click)="mode='material'">+ Material</button></div></div><div class="cards inventory-cards"><article><span>Materials</span><strong>{{materials.length}}</strong><small>Active material records</small></article><article><span>Units in stock</span><strong>{{totalStock}}</strong><small>Selected warehouse</small></article><article><span>Low stock</span><strong>{{lowStock}}</strong><small>At or below reorder level</small></article><article><span>Dispatches</span><strong>{{dispatches.length}}</strong><small>Recorded dispatches</small></article></div><div class="toolbar sub"><div><h3>Warehouse stock</h3><span class="muted">{{warehouseName}}</span></div><div><select class="warehouse" [(ngModel)]="warehouseId" (ngModelChange)="reload()"><option *ngFor="let w of warehouses" [ngValue]="w.id">{{w.code}} — {{w.name}}</option></select><button *ngIf="canManage" (click)="mode='stock'">+ Stock In</button></div></div><table><thead><tr><th>SKU</th><th>Material</th><th>Unit</th><th>Available</th><th>Reorder</th><th>Status</th><th *ngIf="canManage">Action</th></tr></thead><tbody><tr *ngFor="let m of materials"><td>{{m.sku}}</td><td><b>{{m.name}}</b><div class="muted">{{m.description||'—'}}</div></td><td>{{m.unit}}</td><td><b>{{m.quantity||0}}</b></td><td>{{m.reorderLevel||0}}</td><td><span class="pill">{{m.status}}</span></td><td *ngIf="canManage"><button (click)="selectedId=m.id||0;mode='adjust'">Adjust</button></td></tr></tbody></table><div class="empty" *ngIf="!materials.length">No materials in this warehouse.</div><div class="toolbar sub"><h3>Ledger history</h3></div><table *ngIf="transactions.length"><thead><tr><th>Time</th><th>Material</th><th>Warehouse</th><th>Type</th><th>Quantity</th><th>Reference</th></tr></thead><tbody><tr *ngFor="let t of transactions"><td>{{t.createdAt|date:'medium'}}</td><td>{{materialName(t.productId)}}</td><td>{{warehouseLabel(t.warehouseId)}}</td><td>{{t.transactionType}}</td><td [class.negative]="t.quantity<0">{{t.quantity}}</td><td>{{t.referenceType||'—'}} {{t.referenceId?'#'+t.referenceId:''}}</td></tr></tbody></table><div class="empty" *ngIf="!transactions.length">No stock transactions recorded.</div><div class="toolbar sub"><h3>Dispatch history</h3><button (click)="startDispatch()">+ Dispatch</button></div><table *ngIf="dispatches.length"><thead><tr><th>Dispatch</th><th>Customer</th><th>Reference</th><th>Date</th><th>Status</th><th>Items</th></tr></thead><tbody><tr *ngFor="let d of dispatches"><td><b>{{d.dispatchNumber}}</b></td><td>{{customerName(d.customerId)}}</td><td>{{d.referenceNumber}}</td><td>{{d.dispatchDate|date:'medium'}}</td><td><span class="pill">{{d.status}}</span></td><td>{{d.items.length}}</td></tr></tbody></table><div class="empty" *ngIf="!dispatches.length">No dispatches yet.</div></div><section *ngIf="mode" class="modal"><form (ngSubmit)="submit()"><h2>{{mode==='warehouse'?'New warehouse':mode==='material'?'New material':mode==='stock'?'Stock in':mode==='adjust'?'Stock adjustment':'Dispatch materials'}}</h2><ng-container *ngIf="mode==='warehouse'"><input required placeholder="Warehouse code" [(ngModel)]="warehouseDraft.code" name="warehouseCode"><input required placeholder="Warehouse name" [(ngModel)]="warehouseDraft.name" name="warehouseName"></ng-container><ng-container *ngIf="mode==='material'"><input required placeholder="Material code / SKU" [(ngModel)]="draft.sku" name="sku"><input required placeholder="Material name" [(ngModel)]="draft.name" name="name"><input placeholder="Description" [(ngModel)]="draft.description" name="description"><input required placeholder="Unit (PCS, EA, KG...)" [(ngModel)]="draft.unit" name="unit"><input type="number" min="0" placeholder="Initial quantity" [(ngModel)]="draft.initialQuantity" name="initialQuantity"><input type="number" min="0" placeholder="Minimum stock level" [(ngModel)]="draft.reorderLevel" name="reorderLevel"></ng-container><ng-container *ngIf="mode==='stock'||mode==='adjust'"><select required [(ngModel)]="selectedId" name="selectedId"><option [ngValue]="0">Select material</option><option *ngFor="let m of materials" [ngValue]="m.id">{{m.sku}} — {{m.name}} ({{m.quantity||0}})</option></select><input type="number" [min]="mode==='stock'?1:-999999" placeholder="Quantity" [(ngModel)]="quantity" name="quantity"><input required placeholder="Reference / reason" [(ngModel)]="reference" name="reference"><small *ngIf="mode==='adjust'">Positive adds stock; negative removes stock.</small></ng-container><ng-container *ngIf="mode==='dispatch'"><select required [(ngModel)]="customerId" name="customerId"><option [ngValue]="0">Select customer</option><option *ngFor="let c of customers" [ngValue]="c.id">{{c.name}} ({{c.code}})</option></select><div class="line" *ngFor="let line of dispatchLines;let i=index"><select required [(ngModel)]="line.productId" [name]="'product'+i"><option [ngValue]="0">Select material</option><option *ngFor="let m of materials" [ngValue]="m.id">{{m.sku}} — {{m.name}} ({{m.quantity||0}})</option></select><input type="number" required min="1" [(ngModel)]="line.quantity" [name]="'qty'+i" placeholder="Quantity"><button type="button" (click)="removeLine(i)" *ngIf="dispatchLines.length>1">Remove</button></div><button type="button" (click)="addLine()">+ Add material</button><input required placeholder="Sales order / reference" [(ngModel)]="reference" name="dispatchReference"><input placeholder="Delivery address" [(ngModel)]="address" name="address"><textarea placeholder="Notes" [(ngModel)]="notes" name="notes"></textarea></ng-container><div class="actions"><button type="button" (click)="mode=''">Cancel</button><button type="submit">Save</button></div></form></section>`,styles:[`.sub{margin-top:28px}.inventory-cards{margin-bottom:10px}.muted{font-size:12px;color:#667085}.warehouse{padding:9px;border:1px solid #d0d5dd;border-radius:8px}textarea{width:100%;min-height:80px;padding:11px;font:inherit}.line{display:grid;grid-template-columns:1fr 130px auto;gap:8px;margin-bottom:8px}.negative{font-weight:700}`]})
export class InventoryComponent implements OnInit {api=inject(InventoryApi);crm=inject(CrmApi);materials:Material[]=[];warehouses:Warehouse[]=[];customers:Customer[]=[];dispatches:any[]=[];transactions:InventoryTransaction[]=[];mode='';selectedId=0;warehouseId=0;quantity=0;reference='';customerId=0;address='';notes='';canManage=true;warehouseDraft={code:'',name:''};draft:Material={sku:'',name:'',unit:'PCS',initialQuantity:0,reorderLevel:0};dispatchLines:DispatchLine[]=[{productId:0,warehouseId:0,quantity:1}];async ngOnInit(){try{this.warehouses=await this.api.warehouses();this.warehouseId=this.warehouses.find(w=>w.status==='ACTIVE')?.id||0;this.customers=await this.crm.customers('');this.draft.warehouseId=this.warehouseId;await this.reload();}catch(e){alert(e)}}get warehouseName(){return this.warehouses.find(w=>w.id===this.warehouseId)?.name||'Not configured';}get totalStock(){return this.materials.reduce((a,m)=>a+(m.quantity||0),0);}get lowStock(){return this.materials.filter(m=>(m.quantity||0)<=(m.reorderLevel||0)).length;}customerName(id:number){return this.customers.find(c=>c.id===id)?.name||`Customer #${id}`;}materialName(id:number){return this.materials.find(m=>m.id===id)?.name||`Material #${id}`;}warehouseLabel(id:number){return this.warehouses.find(w=>w.id===id)?.code||`Warehouse #${id}`;}async reload(){if(!this.warehouseId)return;this.draft.warehouseId=this.warehouseId;[this.materials,this.dispatches,this.transactions]=await Promise.all([this.api.materials(this.warehouseId),this.api.history(),this.api.transactions()]);}startDispatch(){this.dispatchLines=[{productId:0,warehouseId:this.warehouseId,quantity:1}];this.mode='dispatch';}addLine(){this.dispatchLines.push({productId:0,warehouseId:this.warehouseId,quantity:1});}removeLine(i:number){this.dispatchLines.splice(i,1);}async submit(){try{if(this.mode==='warehouse'){const w=await this.api.createWarehouse(this.warehouseDraft.code,this.warehouseDraft.name);this.warehouses=[...this.warehouses,w];this.warehouseId=w.id;}if(this.mode==='material')await this.api.createMaterial({...this.draft,warehouseId:this.warehouseId});if(this.mode==='stock')await this.api.stockIn(this.selectedId,this.warehouseId,this.quantity,this.reference);if(this.mode==='adjust')await this.api.adjustment(this.selectedId,this.warehouseId,this.quantity,this.reference);if(this.mode==='dispatch'){if(!this.customerId||this.dispatchLines.some(x=>!x.productId||x.quantity<=0))throw new Error('Customer and all dispatch lines are required');await this.api.dispatch({customerId:this.customerId,referenceNumber:this.reference,deliveryAddress:this.address,notes:this.notes,items:this.dispatchLines.map(x=>({...x,warehouseId:this.warehouseId}))});}this.mode='';this.draft={sku:'',name:'',unit:'PCS',initialQuantity:0,reorderLevel:0,warehouseId:this.warehouseId};this.warehouseDraft={code:'',name:''};this.quantity=0;this.reference='';this.customerId=0;this.address='';this.notes='';await this.reload();}catch(e:any){alert(e?.error?.message||e?.message||'Request failed')}}}
